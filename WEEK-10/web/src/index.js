import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (JavaScript + XML que no caso é a sintaxe do HTML. Logo JS + HTML).
